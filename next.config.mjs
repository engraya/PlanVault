/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['bootstrapmade.com', 'images.unsplash.com', 'unsplash.com', 'cdn.rareblocks', 'randomuser.me'],
      },
};

export default nextConfig;
